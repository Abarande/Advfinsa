requests
python-dotenv
openpyxl        # (if working with Excel directly)
pandas          # (optional—only if using DataFrames)
beautifulsoup4  # (optional—only if HTML parsing in Python)
lxml            # (optional—only if HTML parsing in Python)
pandas>=1.2.0
playwright>=1.28.0
python-dotenv>=0.19.0
requests>=2.25.0      # only if you have any other requests-based code
